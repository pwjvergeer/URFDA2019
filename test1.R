## Start demo sjizzle
## Pim
## 11-april
x <- c(1:10) ##c() is a concatenate (make) function

y <- c(1:10)
z <- c(x,y) ## use ctrl+enter to run in console

class(x)
Library(tidyverse)
Dataframe <- tibble ( x = x, y = y)
## letters is a standard vector (alphabet)

## tibble(function for dataframe) column x is x and y is y
df <- data.frame ( x = x, y = y)
tbl <- tibble ( x = x, y = y)
tbl
df

##tibble vs data.frama depends on the flavour you like

